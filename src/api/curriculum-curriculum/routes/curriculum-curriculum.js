'use strict';

/**
 * curriculum-curriculum router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::curriculum-curriculum.curriculum-curriculum');
