'use strict';

/**
 * curriculum-subject service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::curriculum-subject.curriculum-subject');
