'use strict';

/**
 * curriculum-subject router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::curriculum-subject.curriculum-subject');
