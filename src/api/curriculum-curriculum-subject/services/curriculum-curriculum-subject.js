'use strict';

/**
 * curriculum-curriculum-subject service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::curriculum-curriculum-subject.curriculum-curriculum-subject');
