'use strict';

/**
 * curriculum-curriculum-subject router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::curriculum-curriculum-subject.curriculum-curriculum-subject');
