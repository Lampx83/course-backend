'use strict';

/**
 * curriculum-curriculum-subject controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::curriculum-curriculum-subject.curriculum-curriculum-subject');
